server:
  port: 9001

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: http://192.168.1.79:1111/
      #server-addr: localhost:8848

management:
  endpoints:
    web:
      exposure:
        include: "*"
